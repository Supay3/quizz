{% form_theme form 'admin/form_theme/admin_question_form_theme.html.twig' %}

{{ form_start(form) }}
    {{ form_row(form) }}
    <button class="btn btn-success">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
